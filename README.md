# Event-Management-API

📌 Event Management API

A simple Django REST Framework project for managing users and events.
Currently, the project includes a custom user system with registration and user management endpoints.

🚀 Features (Current)
✔ Users

Custom User model (extends AbstractUser)

Unique email authentication
